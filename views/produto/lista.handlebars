
    <div class="row pl-3 pt-3 mt-3">
        <h2>Lista de Produto</h2>
    </div>

  <table class="table">
    <thead>
      <tr>
        <th>Codigo</th>
        <th>Nome</th>
        <th>Peço</th>
        <th>Descrição</th>
        <th></th>
        <th></th>
      </tr>
    </thead>
    <tbody>
       {{#each prod }}
      <tr>
           <td >{{_doc.txcdprod}}</td>
     
           <td >{{_doc.txnmprod}}</td>
      
           <td >{{_doc.txpcprod}}</td>
      
           <td >{{_doc.txdcprod}}</td>
           <td > <button class="btn btn-primary">  Editar  </button> </td>
           <td > <button class="btn btn-danger">  Excluir  </button> </td>
      </tr>
      {{/each}} 
 
    </tbody>
  </table>

